apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lab-03-mern-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    lab: "lab-03"
    app-type: "mern-stack"
    managed-by: "parent-app"
  annotations:
    # Sync wave: Deploy in wave 1 (after databases)
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  
  source:
    repoURL: https://github.com/awaiskhan1/kubernetes.git
    targetRevision: HEAD
    path: helm/mern-helm
    
    helm:
      valueFiles:
        - values.yaml
      
      parameters:
        - name: backend.replicaCount
          value: "1"
        - name: frontend.replicaCount
          value: "1"
        - name: ingress.host
          value: "lab03-mern.local"
  
  destination:
    server: https://kubernetes.default.svc
    namespace: lab-03-mern
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
